# Root template (copy values into app-specific env files)
#
# apps/api/.env
# - DATABASE_URL, JWT secrets, CORS, PORT
#
# apps/web/.env.local
# - NEXT_PUBLIC_API_URL, NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME (later phases)
